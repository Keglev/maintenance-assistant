# Copy to .env and paste the real keys. .env is gitignored and must stay so.
# A provider whose key is missing is skipped with a message; the other one is
# still evaluated.
#
# Nebius Token Factory — https://tokenfactory.nebius.com/ (base URL is in spike.py)
NEBIUS_API_KEY=
# IONOS AI Model Hub — token from the IONOS Cloud console (DCD)
IONOS_API_KEY=
