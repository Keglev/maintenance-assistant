Flyway migration location. Empty in Phase 1 on purpose — the schema for machine,
protocol and chunk (per docs/DOMAIN-MODEL.md) is added in Phase 2 as V1__*.sql.

Keeping the directory present stops Flyway from warning about a missing location.
