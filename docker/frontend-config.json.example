{
  "_comment": "SHAPE ONLY — every value below is a placeholder. Copy to frontend-config.json on the host and fill in the real ones; see docker/README.md, 'Files the pipeline never deploys'. The development copy with working local values is frontend/public/config.json.",
  "keycloakIssuer": "https://auth.example.invalid/realms/<realm-name>",
  "keycloakClientId": "<public-client-id>",
  "apiBaseUrl": "/api"
}
