PROJECT PHASES — maintenance-assistant (Wartungsassistent)
Repo: https://github.com/Keglev/maintenance-assistant
(This file + DECISIONS.txt are handed to every AI session as context.
Naming update overriding older docs: project = "maintenance-assistant",
Keycloak realm = "maintenance". English repo name is intentional.)

=====================================================================
CURRENT STATUS — 2026-08-07, end of session (SESSION HANDOVER)
=====================================================================
Phases 0, 1 and 2 are DONE. Release v0.1.0 is tagged (v0.2.0 tag for
Phase 2 is Carlos's call). Next: PHASE 3.

Phase 2 shipped in PRs #18 (Flyway V1 + machines), #19 (150-protocol
corpus + seed loader), #20 (backups), #21 (ingestion pipeline), #22
(prod compose fix), all merged, plus one ops session (no commits).
PRODUCTION IS INDEXED: 150 protocols INDEXED, 0 FAILED, 166 chunks
(all 1024 dims), 150 documents on the volume (uid 1001), retrieval
verified live (all four E-47 protocols top the Presse-3 ranking).
Cost of full corpus embedding: EUR 0.00055 (27,713 input tokens,
150 calls, 52 s with 4 workers). Seed + backlog flags are DISARMED;
backups now carry real content (dump 752K incl. vectors, verified).

Live demo:  https://maintenance.smartsupply.com.de  (Angular + Keycloak
            SSO, valid TLS via Caddy/Let's Encrypt, demo users
            operator/techniker/schichtleiter/admin, password demo1234)
Auth:       https://auth.smartsupply.com.de (Keycloak, prod mode)
Docs site:  https://keglev.github.io/maintenance-assistant/
Server:     Hetzner CX33 (x86_64, 4 vCPU, 8 GB), Nuremberg, Ubuntu
            26.04. Stack at /opt/maintenance-assistant (compose:
            caddy, frontend, backend, keycloak, postgres+pgvector).
            Secrets in /opt/maintenance-assistant/.env.prod (0600),
            copies in Carlos's password manager. SSH: root + deploy
            user (deploy = CI key). Hetzner firewall: TCP 22/80/443.
CI/CD:      push to main -> CI -> GHCR (:latest + :sha) -> ssh deploy
            -> public health verify. Manual redeploy: workflow_dispatch.
LLM:        ADR-002 ACCEPTED — IONOS AI Model Hub primary (Berlin,
            de-txl), Nebius Token Factory fallback. Embeddings
            BAAI/bge-m3 (1024 dims), chat Llama-3.3-70B-Instruct
            (quality) / Qwen3.5-9B (fast, needs reasoning_effort=
            "none"). Threshold = config property, initial 0.583.
            IONOS gateway needs explicit encoding_format; ignore
            *-migration model aliases. Evidence: spike/adr-002/
            RESULTS.md + PR #14. No provider hard cap — EUR 7 cost
            alert set; NFR-7 rests on the app layer (by design).
Keys:       PRODUCTION: 365-day IONOS token (created 2026-08-07,
            expires 2027-08-07) lives in the server .env.prod and in
            Carlos's password manager — nowhere else. ROTATION
            REMINDER: calendar entry ~2 weeks before 2027-08-07;
            rotation = edit .env.prod + `up -d --force-recreate
            backend` (see OPEN ITEMS: env edits are inert until
            recreate). The old 30-day spike token (exp ~2026-09-05)
            and the Nebius trial key remain in spike/adr-002/.env
            (gitignored, local) for spike reruns only.
            Token paste procedure that works (after two mangled
            attempts — doubled+truncated at 4095 chars): password
            manager -> Notepad (visual check: one line, eyJ..., two
            dots) -> scp to server -> tr -d '\r\n' into .env.prod ->
            verify dots=2, length ~1712, prefix eyJ.
Config:     Flyway auto-config was DEAD on Boot 4.1 (missing
            spring-boot-flyway module) — fixed and empirically
            verified in PR #16. spring.flyway.* now binds. Error
            properties moved to spring.web.error.*.

OPEN ITEMS carried forward (not blockers, do not lose):
- Re-validate chat latency from the Hetzner host during Phase 3
  (spike figures are from a residential connection; ADR-002 notes it).
- Threshold 0.583: MEASURED 2026-08-07 against the real corpus and it
  is TOO HIGH. E-47 demo 0.695 (Mode A, ok), cross-language demo 0.577
  (wants Mode A, would be refused), Mode-B gap 0.502 (correctly Mode B).
  Set it near 0.54-0.55 in Phase 3. Evidence: ADR-002 dated note.
- Backups: DONE 2026-08-07. Nightly systemd timer on the host at
  03:15 UTC: pg_dump (custom format) of the app DB + tar.gz of the
  protocol-files volume, both self-verified, keep 14, in
  /opt/maintenance-assistant/backups. Scripted in infra/backup.sh +
  infra/install-backup.sh, documented in arc42 §7.6 incl. the Hetzner
  snapshot routine. STILL OPEN by decision: no offsite copy and no
  restore automation (arc42 §7.8).
- Citation few-shot prompt must NOT apply to the Mode-B/refusal path
  (observed leakage in the spike). DONE in the query PR: two separate
  prompt classes, and Mode B's schema has no source field at all.
  NOTE for anyone editing them: Mode B does need a SHAPE example of
  its own (no source/label/protocol in it) or constrained decoding
  runs away on whitespace — measured, twice, at two token caps.
- NEW, and the sharpest number in the project: the cross-language demo
  clears the threshold by 0.0066 (0.5566 against 0.55). It is the
  first thing to re-measure after any change to chunking, the corpus
  or the embedding model. Reproduce with QueryDemoVerificationIT
  (skipped unless LLM_API_KEY is set).
- NEW: chat latency median ~13 s end to end, and 1 of 13 measured
  queries breached NFR-4's 30 s ceiling at 38.9 s — from a
  residential connection. Re-measuring from the Hetzner host was
  already an open item; it is now the urgent half of it, because the
  answer is no longer comfortably inside the ceiling.
- OPS RULE (learned the hard way, cost ~12 min): the deploy pipeline
  pulls IMAGES ONLY — it never syncs docker-compose.prod.yml or
  .env.prod to the host. Any merged change to those files must be
  applied on the server BY HAND before/with the next deploy (#22 was
  merged in git but absent on the host until an ops session copied
  it; rollback copy at docker-compose.prod.yml.bak-20260807-162246).
  DOCS DEBT: DONE — arc42 07, "CI deploys images, never configuration",
  including the .bak rule.
- OPS RULE 2: an .env.prod edit is INERT until the container is
  recreated (`up -d --force-recreate backend`); env freezes at
  container creation. A container once ran 150 calls into a 401 with
  a stale key while the file was already correct. DOCS DEBT: DONE —
  arc42 §7.4, "An .env.prod edit is inert until the container is
  recreated", with the printenv check.
- LLM_CHAT_MODEL empty default in docker-compose.prod.yml: FIXED in
  the query PR — it now repeats application.yml's real default
  (meta-llama/Llama-3.3-70B-Instruct), and LLM_CHAT_DAILY_CALL_BUDGET
  joins it. It stopped being harmless the moment the chat path read it.
- ACTION FOR THE NEXT DEPLOY, and it is the ops rule above in
  practice: the query PR changes docker-compose.prod.yml, so merging
  it deploys the new image with the OLD compose file. Copy the file to
  the host (with a .bak) and `up -d --force-recreate backend`, or the
  chat model stays empty and every answer fails.
- CHUNK COUNT, canonical: 150 protocols -> 166 chunks (134x1 + 16x2;
  the arithmetic only works for 166). PR #21's summary and an older
  DECISIONS.txt line said 151/167 — off by one, likely a leftover
  test upload counted during local verification. Production measured
  150/166 with token counts matching local exactly. Use 150/166.
- Java base package DECIDED 2026-08-07: KEEP com.keglevich.
  maintenanceassistant. Robots must NOT rename it (see DECISIONS.txt
  == ARCHITECTURE ==).
- Editor hygiene (Carlos, no repo change): Spring Boot Extension Pack
  in VS Code clears the false "never used" warnings; select the
  Python interpreter for spike/ imports.

NEXT SESSION STARTS WITH: Phase 3 PR 2, the Angular search view.
  (1) feat/query-api — DONE (PR open, not merged). POST /api/query for
      operator/techniker/schichtleiter; question embedded, one
      machine-filtered pgvector top-5, threshold 0.55 -> Mode A / B.
      Chat = plain RestClient (Spring AI revisited on the chat path
      and declined again — see DECISIONS.txt == QUERY PATH ==).
      Llama-3.3-70B FOR EVERY ROLE on measured evidence; Qwen3.5-9B
      failed to ground the cross-language demo. Two separate prompt
      classes, structured JSON, citations validated against the
      retrieved set. NFR-7: Bucket4j 10/min per sub, chat_budget
      table (V3), max_tokens 1200 in the client, Caffeine cache keyed
      including the ROLE. Housekeeping done: compose default + both
      arc42 rules. STILL OPEN from it: latency from the Hetzner host,
      and the 0.0066 cross-language margin (both in OPEN ITEMS).
  (2) feat/search-ui — Angular search view: Mode A/B visually
      distinct, source links, upload view with status, DE/EN switch.
      The response DTO is already shaped for it: mode, answer with
      inline [P1] markers, language, claims (text + source label) and
      citations (label, protocolId, title, errorCode, incidentDate,
      similarity). Mode B returns empty claims and citations.
      NOTE: the view needs a machine picker and there is no endpoint
      listing machines yet — /api/query takes the machine UUID.
  Then Phase 4 (polish + ship, v1.0.0).

=====================================================================
GIT WORKFLOW (MANDATORY FOR EVERY AI SESSION — no exceptions)
=====================================================================
- Work on exactly ONE branch, created from up-to-date main:
    git checkout main && git pull && git checkout -b <type>/<short-name>
  Never create additional branches. Never commit or push to main
  (branch protection rejects it anyway).
- Commit in small logical units as you work, not one commit at the end.
- Conventional Commits: type(scope): imperative summary, max 72 chars.
  Types: feat, fix, chore, docs, ci, test, refactor.
  Scopes: backend, frontend, docker, docs, ci.
  Body (wrapped ~72 chars) explaining WHY when not self-evident.
  Never mention AI, Claude, or "generated" in commit messages.
- Before finishing: run `git log --oneline main..HEAD`, show the list,
  reword anything vague (no "update files", no "changes").
- Push the branch and open the pull request yourself:
    gh pr create --base main --title "<conventional title>" \
      --body "<markdown: What / Why / Verification / Out of scope>"
- Print the PR URL as final output. NEVER merge — merging is done
  manually by Carlos after review.
- After every merge Carlos refreshes local main with
  `git fetch && git reset --hard origin/main` (squash merges — never
  plain `git pull` after a merge).

RULES CARRIED ACROSS SESSIONS
- Demo must always work; test before every application sent out
- Every architecture change gets an ADR (docs/adr/, use ADR-TEMPLATE.md)
- Requirements v0.2 (7 NFRs) and DECISIONS.txt are the source of truth
- Docs live in docs/arc42 + docs/adr; published site = gh-pages branch
- Releases: manual tags v* by Carlos; release.yml creates the GitHub
  Release. Robots never create tags.

=====================================================================
PHASE 0 — PREP  [DONE]
=====================================================================
[x] Fix stockease: demo login + reachable API docs (recruiter feedback)
[x] AI-USAGE.md added
[x] ADR-002 spike (1 day): Nebius vs IONOS — multilingual embedding
    DE<->EN retrieval test, citation prompt test, cost per query,
    billing limits/prepaid. Done in spike/adr-002/ (two rounds, both
    providers live); ADR-002 is Accepted (2026-08-06) and the spike
    REVERSED the proposal: IONOS primary, Nebius documented fallback.
    Billing checked manually: no hard spending cap anywhere, only cost
    alerts (EUR 7 alert configured) -> NFR-7 rests on the app layer.
    Spring AI vs Boot 4.1 is NOT closed by this spike (Python on
    purpose, so it measured the provider not the Java integration);
    it moves to Phase 3, fallback stays a plain RestClient.

=====================================================================
PHASE 1 — WALKING SKELETON  [DONE — Release v0.1.0, 2026-08-06]
=====================================================================
[x] Repo structure: frontend/, backend/, docs/, docker/, .github/
    (only README.md at repo root)
[x] docs: arc42 skeleton, ADR-001/-002/-003/-004 all Accepted,
    docs theme (docs/_theme, Pandoc), mermaid script (docs/scripts)
[x] docker-compose: Postgres(pgvector) + Keycloak, realm "maintenance",
    4 roles (operator/techniker/schichtleiter/admin), demo users,
    public client "frontend" (Auth Code Flow + PKCE), realm-export.json
[x] Branch protection on main (PRs required), squash merges; single
    required check "build-and-test" (see partition below)
[x] Backend skeleton (Spring Boot 4.1, Java 21, Maven, modules
    ingestion/query/web, /api/health public, /api/hello JWT-secured,
    springdoc-openapi, JaCoCo, multi-stage Dockerfile)
[x] Frontend skeleton (Angular 22 standalone, angular-oauth2-oidc PKCE,
    /login + guarded /home, test runner: vitest (jsdom, no browser),
    coverage in coverage/, Compodoc)
[x] CI/CD pipelines (see PIPELINE ARCHITECTURE below)
[x] GitHub Pages configured — docs site live on gh-pages
[x] Production deploy: Hetzner CX33 (x86 — hosting revision: CAX/ARM
    price advantage disappeared 2026-06 + out of stock at order time;
    CI still builds multi-arch so ARM stays an option), Caddy TLS,
    Keycloak prod mode, full stack via docker compose; CI deploys
    over ssh with public-URL health verification
[x] DONE criterion met: login flow runs on the VPS via pipeline
[x] Release v0.1.0 tagged (after local-main squash-merge cleanup —
    see the reset --hard rule in GIT WORKFLOW)

UNIFIED REQUIRED CHECK — "build-and-test"
Branch protection requires exactly ONE status check name:

    build-and-test

Three workflows gate pull requests, and all three name their job
`build-and-test` (job id, no `name:` key — the id is what GitHub reports).
Whichever part of the repo a PR touches, a job under that name reports on
it. A PR touching two areas gets two checks under the name; both must pass.

The pull_request path lists are a PARTITION and are maintained as a set:

    frontend-ci.yml    paths:        frontend/**
                                     .github/workflows/frontend-ci.yml
    docs-pr-check.yml  paths:        docs/**
                                     .github/workflows/docs.yml
                                     .github/workflows/docs-pr-check.yml
    backend-ci.yml     paths-ignore: frontend/**
                                     docs/**
                                     .github/workflows/docs.yml
                       => catch-all: backend/**, docker/**, spike/**,
                          README.md, .gitignore, AI-USAGE.md, LICENSE,
                          everything else

INVARIANT — do not break this:
- A path may be ignored by backend-ci.yml ONLY if another workflow gives
  that PR a build-and-test check. A path ignored by all three leaves its
  PRs with no required check and permanently unmergeable.
- Root files (README.md, .gitignore, AI-USAGE.md, LICENSE) stay with
  backend-ci on purpose: they live outside docs/, so the docs build never
  reads them and would gate them on nothing.
- The gate workflows' own files (frontend-ci.yml, docs-pr-check.yml) are
  deliberately NOT ignored by backend-ci.yml, so editing a gate runs the
  backend suite too. Getting these lists wrong is how the repo loses its
  ability to merge.
- push triggers are NOT a partition: they exist to produce artifacts and
  fire the deploy workflows, so they stay narrow (backend-ci on
  backend/**, frontend-ci on frontend/**). Widening them would publish a
  new image for a README typo.

PIPELINE ARCHITECTURE (.github/workflows/)
1. frontend-ci.yml    PR (paths frontend/**) + push main: job
                      build-and-test — test with coverage (test runner:
                      vitest in jsdom — no browser needed on the runner),
                      build; upload coverage + Compodoc artifacts on main
2. backend-ci.yml     PR (catch-all, see partition above) + push main
                      (paths backend/**): job build-and-test — mvn verify
                      (JUnit + JaCoCo), OpenAPI spec; upload artifacts
                      on main
2b. docs-pr-check.yml PR (paths docs/** + the docs workflow files): job
                      build-and-test — renders the mermaid diagrams, builds
                      the site with build-docs.sh and link-checks it with
                      lychee. Publishes nothing.
3. frontend-deploy.yml / backend-deploy.yml
                      workflow_run success of the matching CI on main,
                      or manual workflow_dispatch for a redeploy without
                      a code change. Two jobs:
                        image  - multi-arch docker build (amd64+arm64)
                                 -> GHCR, tags :latest and :sha-<commit>
                        deploy - ssh to the Hetzner host as the deploy
                                 user, `compose pull <svc> && up -d <svc>`,
                                 then verify over the PUBLIC url (so DNS,
                                 TLS and Caddy are exercised too) and fail
                                 the job if it does not come back healthy
                      Both deploy jobs share the concurrency group
                      "hetzner-deploy" with cancel-in-progress: false, so
                      rollouts to the server serialise and a superseded
                      build never cancels one in flight.
                      Secrets (repo level): DEPLOY_SSH_KEY, DEPLOY_HOST,
                      DEPLOY_USER. The key is a dedicated ed25519 pair
                      used by CI only; it is passed through the step env,
                      never interpolated into a command line, and removed
                      in an always() step.
4. docs.yml           a) push main paths docs/**: regenerate mermaid
                         diagrams, build the site with Pandoc via
                         .github/scripts/docs/build-docs.sh -> gh-pages
                         (theme: docs/_theme, landing + arc42 + ADRs)
                      b) workflow_run success of frontend-ci/backend-ci:
                         publish coverage + API docs into gh-pages
                         subfolders (/frontend/coverage, /frontend/
                         api-docs, /backend/coverage, /backend/api-docs)
                         WITHOUT deleting the rest (keep_files)
5. release.yml        tag v*: GitHub Release with generated notes

=====================================================================
PHASE 2 — DATA + INGESTION  [DONE — 2026-08-07, PRs #18-#22 + ops]
=====================================================================
[x] Flyway migrations: machine, protocol, chunk (vector(1024)), HNSW
    vector index (choice documented in ADR-004 dated note), 10-machine
    repeatable seed with fixed UUIDs (#18)
[x] Synthetic corpus: 150 protocols (108 DE / 42 EN, 120 STOERUNG /
    30 WARTUNG, fault taxonomy MATERIAL 23 / BEDIENUNG 29 / TECHNIK 68
    across 9 subtypes, quality mix 90/45/15). Demo seeds: E-47 on
    Presse 3 x4 with four DIFFERENT root causes, Mode-B gap on AB-02
    (dosing), DE-query->EN-protocol on FB-04 (mistracking). Mixed-
    language protocols included (EN alarm strings inside DE prose).
    Seed loader = flag-gated ApplicationRunner, idempotent (#19)
[x] Ingestion pipeline: upload (schichtleiter, 202), file -> volume,
    paragraph chunking, bge-m3 via plain RestClient (Spring AI CLOSED
    — see DECISIONS.txt), RECEIVED -> INDEXED/FAILED async, backlog
    catch-up, client-side budget counter (NFR-7), Testcontainers (#21)
[x] Prod wiring fix: MAINTENANCE_FILES_PATH, flag passthrough,
    empty-override defaults (#22) — and applied BY HAND on the host,
    because CI never syncs compose files (see OPEN ITEMS)
[x] Production indexed + verified live; flags disarmed (ops session)
[x] Early task: backups (nightly pg_dump + volume archive + snapshot
    routine) — DONE 2026-08-07, see arc42 §7.6
[x] PREREQUISITE ADR-002 — MET. Threshold 0.583 revalidated against
    the real corpus: TOO HIGH, tune to ~0.54-0.55 in Phase 3.

=====================================================================
PHASE 3 — RAG QUERY + MODES
=====================================================================
- Query module: embed question -> filtered pgvector top-5 ->
  threshold -> Mode A/B
- Prompting: answer only from sources, cite each claim (few-shot
  format example; MUST NOT apply to the Mode-B/refusal path — spike
  observed citation leakage into refusals), answer in user's language
  (explicit language-pinning rule incl. refusals), role-based
  filtering (Operator = operator-safe steps only)
- Chat model choice per role finalized here (Llama-3.3-70B vs
  Qwen3.5-9B, spike round-2 evidence); structured JSON output is the
  app-side enforcement fallback
- Spring AI vs plain RestClient decided here (fallback RestClient)
- Fresh long-TTL IONOS token for the server .env.prod
- Re-validate latency from the Hetzner host (ADR-002 residual risk)
- NFR-7 guards: Bucket4j per-user rate limit, daily budget counter,
  max_tokens cap, query cache
- Angular: search view with Mode A/B visual distinction + source
  links; upload view with status; DE/EN switch

=====================================================================
PHASE 4 — POLISH + SHIP
=====================================================================
- Tests (JUnit/Mockito; at least: mode routing, role filtering,
  budget guard)
- OpenAPI/Swagger reachable in the deployed demo (recruiter feedback!)
- README: problem first, 90-second demo walkthrough, architecture
  diagram, links to ADRs and gh-pages docs site; AI-USAGE.md current
- Production is already live on Hetzner CX33 since Phase 1 — Phase 4
  deploy work is verification + hardening, not first deployment
- "How to reproduce" section in spike/adr-002/RESULTS.md (provability)
- Full recruiter walkthrough test on a phone and a fresh browser
- Release v1.0.0

=====================================================================
PHASE 5 — EVOLUTION (after applications are out, optional)
=====================================================================
- Extract ingestion service + Kafka (per ADR-001), document as ADR-005
- Candidates: similar-machine search, OCR stretch goal, downtime
  stats view, Spring Modulith boundary tests, reranker as retrieval
  quality lever (Qwen3-VL-Reranker-8B is already served on IONOS)
